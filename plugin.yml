name: WorldEdit
main: WorldEdit.WorldEdit
version: "1.0"
api: ["1.0.0"]
prefix: "§eWE"

commands:
  /:
    description: View available WorldEdit commands.
    permission: worldedit.command
    aliases:
      - /help
      - /set
      - /cut
      - /cyl
      - /hcyl
      - /walls
      - /sphere
      - /hsphere
      - /replace
      - /copy
      - /paste
      - /pos1
      - /pos2
      - /1
      - /2
      - /undo
      - /redo
      - /wand